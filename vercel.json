{
  "framework": "vite",
  "outputDirectory": "dist",
  "routes": [
    { "source": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "source": "/(.*)", "dest": "/index.html" }
  ]
}
